**Verdict: DOES NOT FULLY SURVIVE.**  
It survives as a **first-pass statistical candidate**, but not as a deployable edge. The report’s `"verdict": "EDGE"` is too strong because the 3.84%/yr OOS net return can be materially reduced or killed by unmodeled short-spot borrow/availability, real execution, and survivor-only universe bias.

Key audit points:

- **Look-ahead:** no fatal look-ahead found. The script enforces `signal_lag_bars >= 1` and uses `position = daily_target.shift(1)` in [cand_funding_carry_xsec_lowturn.py](<D:\量化交易CLAUDE\experiments\cand_funding_carry_xsec_lowturn.py:420>). Artifact check confirmed positions exactly equal the shifted target.
- **Costs:** turnover cost math is internally correct. Reconstructed gross/net PnL matched artifacts to ~1e-16, and OOS gross-net annual drag equals reported turnover cost: ~1.008%/yr. But the cost model only covers 10bp/leg turnover. It omits borrow/short availability for the short-spot side, spread/slippage/impact, margin/liquidation path, and funding timestamp execution risk. OOS average short-spot notional is ~0.349 NAV; a 10% borrow rate costs ~3.49%/yr, almost wiping out the 3.84%/yr reported net.
- **Survivorship:** not clean. The default universe is a static current survivor list in [cand_funding_carry_xsec_lowturn.py](<D:\量化交易CLAUDE\experiments\cand_funding_carry_xsec_lowturn.py:23>), not a point-in-time historical perp universe with delistings/failures. So the result is valid only for “today’s surviving liquid majors,” not the real historical opportunity set.
- **Alignment artifacts:** mostly acceptable but not clean. Spot/perp daily bars are aligned by open_time while returns are close-to-close; the 1-day shift mostly fixes executability. However the run included an unfinished final UTC day, `2026-06-18`, with 6 active missing return cells filled to zero via `_run_engine` fillna behavior in [cand_funding_carry_xsec_lowturn.py](<D:\量化交易CLAUDE\experiments\cand_funding_carry_xsec_lowturn.py:327>). Dropping that row barely changes Sharpe, but the pipeline should exclude unfinished bars.
- **Frozen post-settlement futures:** not a blocker here. This uses perpetual futures, not dated futures; artifact summary reports zero frozen spot/perp bars.
- **DSR / multiple testing:** local DSR passes. The selected OOS SR/period is 0.356 vs DSR benchmark 0.172 over 81 variants, with DSR probability ~0.99998. My sensitivity check still passed at N=10,000 using the same trial dispersion. But this deflates only this script’s 81-grid search, not the whole broader research program / idea mining history.

So: **the low-turnover funding-carry signal looks real enough to keep researching, but the reported backtest does not prove a tradable edge after full implementation costs and universe bias.** The next hardening step is point-in-time universe + borrow/shortability model + 8h event-timestamp backtest + unfinished-bar exclusion.